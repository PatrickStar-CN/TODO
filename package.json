{
  "name": "todo-tools",
  "version": "1.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node scripts/check-state.js",
    "preview": "vite preview",
    "neu:run": "node scripts/sync-config.js && node scripts/ensure-neutralino.js && npm run build && npx @neutralinojs/neu run",
    "neu:build": "node scripts/sync-config.js && node scripts/ensure-neutralino.js && node scripts/check-build-output.js && npm run build && npx @neutralinojs/neu build && node scripts/patch-exe.js",
    "release": "node scripts/release.js"
  },
  "devDependencies": {
    "@neutralinojs/neu": "^11.7.1",
    "rcedit": "^5.0.2",
    "vite": "^6.3.5"
  }
}
